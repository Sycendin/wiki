import { Fragment } from "react";

import styles from "../../../styles/charprofile.module.css";
const CharProfile = ({ data }) => {
  return (
    <Fragment>
      <div className={`${styles.charProfile}`}></div>
    </Fragment>
  );
};
export default CharProfile;
